# 智慧旅游综合服务系统 架构图

## 一、系统总体架构

```mermaid
graph TB
    subgraph 用户层
        Browser[用户浏览器]
        MobileApp[移动端应用]
    end

    subgraph 前端应用层
        VueApp["Vue 3 SPA 应用<br/>TypeScript + TailwindCSS"]
        Router[Vue Router<br/>路由管理]
        PiniaStore["Pinia Store<br/>状态管理"]
    end

    subgraph 功能模块层
        Home[首页模块]
        Community[社区模块]
        Assistant[AI助手模块]
        TrainTickets[火车票模块]
        Profile[用户资料模块]
        Admin[管理后台模块]
    end

    subgraph 服务接口层
        SupabaseClient["Supabase JS SDK"]
        EdgeFunctions["Edge Functions<br/>边缘函数"]
        DjangoAPI["Django RAG API"]
    end

    subgraph Supabase后端服务
        Auth["Auth<br/>身份认证"]
        RealtimeDB["Realtime Database<br/>实时数据库"]
        Storage["Storage<br/>对象存储"]
    end

    subgraph Django RAG服务
        RAGController["RAG控制器"]
        VectorSearch["向量检索<br/>pgvector"]
        FullTextSearch["全文检索<br/>PGroonga"]
        LLMGateway["大模型网关<br/>智谱/DeepSeek/星火"]
    end

    subgraph 外部服务
        Train12306["12306火车票API"]
        AIProviders["AI模型供应商"]
    end

    subgraph 数据存储层
        PostgreSQL["PostgreSQL<br/>主数据库"]
        VectorDB["向量数据库<br/>pgvector"]
        SearchIndex["搜索索引<br/>PGroonga"]
    end

    Browser --> VueApp
    MobileApp --> VueApp

    VueApp --> Router
    VueApp --> PiniaStore

    Router --> Home
    Router --> Community
    Router --> Assistant
    Router --> TrainTickets
    Router --> Profile
    Router --> Admin

    Home --> SupabaseClient
    Community --> SupabaseClient
    Profile --> SupabaseClient
    Admin --> SupabaseClient

    Assistant --> SupabaseClient
    Assistant --> DjangoAPI
    TrainTickets --> SupabaseClient

    SupabaseClient --> Auth
    SupabaseClient --> RealtimeDB
    SupabaseClient --> Storage
    SupabaseClient --> EdgeFunctions

    EdgeFunctions --> AIProviders

    DjangoAPI --> VectorSearch
    DjangoAPI --> FullTextSearch
    DjangoAPI --> LLMGateway
    DjangoAPI --> PostgreSQL

    LLMGateway --> AIProviders

    RealtimeDB --> PostgreSQL
    Storage --> PostgreSQL

    TrainTickets -.-> Train12306

    VectorSearch --> VectorDB
    FullTextSearch --> SearchIndex
```

## 二、前端架构（Vue 3 SPA）

```mermaid
graph LR
    subgraph 视图层 View
        Pages["页面组件<br/>Views/"]
        Components["可复用组件<br/>Components/"]
    end

    subgraph 逻辑层 Logic
        Composables["组合式函数<br/>Composables/"]
        Services["业务服务<br/>Services/"]
    end

    subgraph 状态层 State
        Pinia["Pinia Store<br/>状态管理"]
        RouterState["Vue Router<br/>路由状态"]
    end

    subgraph 基础设施层 Infra
        SupabaseSDK["Supabase Client"]
        Utils["工具函数<br/>Utils/"]
    end

    Pages --> Components
    Pages --> Composables
    Components --> Composables
    Composables --> Services
    Services --> Pinia
    Services --> SupabaseSDK
    Pages --> RouterState
```

## 三、后端服务架构（Supabase BaaS + Django）

```mermaid
graph TB
    subgraph 接入层
        Frontend["Vue 3 前端"]
    end

    subgraph Supabase边缘层
        EdgeFn1["generate-image<br/>文生图"]
        EdgeFn2["analyze-image<br/>图生文"]
    end

    subgraph Supabase核心服务
        Auth["身份认证服务"]
        Database["PostgreSQL<br/>实时数据库"]
        Storage["对象存储服务"]
        Realtime["实时订阅服务"]
    end

    subgraph Django RAG服务
        DjangoApp["Django应用"]
        RagEngine["RAG引擎"]
        VectorIndex["向量检索<br/>match_knowledge_base"]
        LLMRouter["大模型路由"]
    end

    subgraph AI供应商
        Zhipu["智谱GLM"]
        DeepSeek["DeepSeek"]
        Spark["讯飞星火"]
    end

    subgraph 数据层
        PG["PostgreSQL<br/>关系数据"]
        VectorPG["pgvector<br/>向量数据"]
        SearchPG["PGroonga<br/>全文索引"]
    end

    Frontend --> Auth
    Frontend --> Database
    Frontend --> EdgeFn1
    Frontend --> EdgeFn2
    Frontend --> DjangoApp

    EdgeFn1 --> Storage
    EdgeFn2 --> AIProviders

    DjangoApp --> RagEngine
    RagEngine --> VectorIndex
    RagEngine --> LLMRouter
    LLMRouter --> Zhipu
    LLMRouter --> DeepSeek
    LLMRouter --> Spark

    VectorIndex --> VectorPG
    RagEngine --> PG
    DjangoApp --> SearchPG
```

## 四、部署架构

```mermaid
graph TB
    subgraph CDN网络
        CDN["CDN分发网络<br/>静态资源全球加速"]
    end

    subgraph 边缘节点
        EdgeJP["东京边缘节点"]
        EdgeSG["新加坡边缘节点"]
        EdgeUS["美西边缘节点"]
    end

    subgraph 云服务端
        SupabaseCloud["Supabase托管云<br/>PostgreSQL + Redis"]
        DjangoCloud["Django服务<br/>Railway/Render"]
    end

    subgraph 外部API
        TrainAPI["12306官方API"]
        AIVendors["AI服务商API"]
    end

    CDN --> EdgeJP
    CDN --> EdgeSG
    CDN --> EdgeUS

    EdgeJP --> SupabaseCloud
    EdgeJP --> DjangoCloud

    DjangoCloud --> TrainAPI
    DjangoCloud --> AIVendors

    SupabaseCloud --> PostgreSQL
    SupabaseCloud --> VectorPG
    SupabaseCloud --> Storage
```

## 五、技术选型说明

### 5.1 前端技术选型

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue 3 | 3.3+ | 前端框架，响应式UI |
| TypeScript | 5.0+ | 类型安全开发 |
| Vite | 5.0+ | 构建工具，冷启动快 |
| TailwindCSS | 3.3+ | 原子化CSS |
| Vue Router | 4.0+ | 单页路由管理 |
| Pinia | 2.0+ | 状态管理 |
| supabase-js | 2.0+ | 后端服务客户端 |

### 5.2 后端技术选型

| 技术 | 用途 |
|------|------|
| Supabase | BaaS后端服务 |
| PostgreSQL | 主数据库 |
| pgvector | 向量嵌入存储 |
| PGroonga | 中文全文搜索 |
| Django | RAG应用框架 |
| Edge Functions | 无服务器计算 |

### 5.3 AI模型支持

| 模型 | 提供商 | 用途 |
|------|--------|------|
| GLM-4 | 智谱AI | 主力对话模型 |
| DeepSeek-chat | 深度求索 | 辅助对话模型 |
| Spark-4 | 讯飞星火 | 备用对话模型 |

## 六、系统特性

### 6.1 云原生特性

- **弹性伸缩**：Supabase托管服务自动扩缩容
- **全球加速**：CDN边缘节点分发静态资源
- **免运维**：BaaS模式无需管理服务器
- **高可用**：Supabase提供99.9% SLA保障

### 6.2 实时性特性

- **数据库实时订阅**：WebSocket推送数据变更
- **在线状态感知**：用户上线/下线实时显示
- **消息即时送达**：评论、点赞通知秒级推送

### 6.3 安全性特性

- **行级安全策略(RLS)**：数据访问精确控制
- **JWT认证**：安全的身份令牌
- **HTTPS加密**：全站SSL/TLS加密

---

*文档生成时间：2026年3月*
*项目名称：基于Vue 3 + Supabase的云原生智慧旅游综合服务系统*
