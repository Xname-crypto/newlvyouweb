# 智慧旅游综合服务系统 功能流程图

## 一、用户注册登录流程

```mermaid
graph TD
    A[用户打开应用] --> B{已有账号?}
    B -->|是| C[输入邮箱密码]
    B -->|否| D[点击注册]
    C --> E[调用signInWithPassword]
    E --> F{验证成功?}
    F -->|否| G[显示错误信息]
    F -->|是| H[获取用户Session]
    H --> I[获取用户Profile]
    I --> J[路由跳转首页]
    D --> K[输入邮箱密码昵称]
    K --> L[调用signUp]
    L --> M{注册成功?}
    M -->|否| N[显示错误信息]
    M -->|是| O[发送验证邮件]
    O --> P[用户点击邮箱验证链接]
    P --> Q[确认邮箱地址]
    Q --> H
    G --> B
    N --> B
```

## 二、用户发帖流程

```mermaid
graph TD
    A[用户进入发布页面] --> B[选择内容类型]
    B --> C{类型选择}
    C -->|图文| D[上传图片]
    C -->|视频| E[上传视频]
    C -->|文章| F[富文本编辑]
    D --> G{继续添加?}
    G -->|是| D
    G -->|否| H[填写标题和正文]
    E --> H
    F --> H
    H --> I{内容校验?}
    I -->|不通过| J[提示修改]
    I -->|通过| K[提交帖子]
    K --> L[调用posts.insert]
    L --> M[RLS权限验证]
    M --> N{验证通过?}
    N -->|否| O[返回权限错误]
    N -->|是| P[插入数据库]
    P --> Q[返回成功响应]
    Q --> R[刷新社区列表]
    R --> S[跳转到社区页面]
    O --> H
    J --> H
```

## 三、评论与回复流程

```mermaid
graph TD
    A[用户查看帖子] --> B[点击评论按钮]
    B --> C[加载评论列表]
    C --> D[显示根评论]
    D --> E{是回复?}
    E -->|是| F[选择被回复的评论]
    F --> G[输入回复内容]
    E -->|否| H[输入根评论内容]
    G --> I[提交评论]
    H --> I
    I --> J[调用comments.insert]
    J --> K[数据库触发器工作]
    K --> L[自动生成通知]
    L --> M[通知作者]
    M --> N[更新评论列表]
    N --> O[显示新评论]
```

## 四、点赞收藏流程

```mermaid
graph TD
    A[用户点击点赞/收藏按钮] --> B[前端UI立即更新]
    B --> C[显示已点赞状态]
    C --> D[调用toggleInteraction]
    D --> E[调用interactions.insert/update]
    E --> F[RLS权限验证]
    F --> G{验证通过?}
    G -->|否| H[回滚UI状态]
    G -->|是| I[数据库操作成功]
    I --> J[触发器生成通知]
    J --> K[通知内容作者]
    H --> L[显示错误提示]
    K --> M[实时推送更新]
```

## 五、AI助手对话流程

```mermaid
graph TD
    A[用户输入问题] --> B[选择AI模型]
    B --> C[保存用户消息]
    C --> D[调用assistantService.chat]
    D --> E[发送到Django RAG API]
    E --> F[向量相似度检索]
    F --> G[全文关键词检索]
    G --> H[RRF混合排序]
    H --> I[选取Top-K上下文]
    I --> J[构建Prompt]
    J --> K[调用大模型API]
    K --> L[流式返回结果]
    L --> M[保存AI响应]
    M --> N[更新会话列表]
    N --> O[显示AI回答]
```

## 六、RAG检索增强流程

```mermaid
graph TD
    A[用户Query输入] --> B[向量嵌入生成]
    B --> C[向量相似度搜索]
    A --> D[关键词抽取]
    D --> E[PGroonga全文搜索]
    C --> F[向量Top-K结果]
    E --> G[全文Top-K结果]
    F --> H[RRF融合计算]
    G --> H
    H --> I[综合排序结果]
    I --> J[选取N条最优片段]
    J --> K[构建增强Prompt]
    K --> L[调用LLM生成回答]
    L --> M[返回最终Response]
```

## 七、火车票查询流程

```mermaid
graph TD
    A[用户进入火车票页面] --> B[选择出发地和目的地]
    B --> C[选择出发日期]
    C --> D[点击查询按钮]
    D --> E[调用12306接口]
    E --> F{接口可用?}
    F -->|否| G[显示网络错误]
    F -->|是| H[获取车票列表]
    H --> I[解析车次信息]
    I --> J[显示车次列表]
    J --> K{用户选择车次?}
    K -->|是| L[获取座位信息]
    L --> M[用户悬停查看价格]
    M --> N[动态获取价格详情]
    K -->|否| O[继续浏览]
    N --> P[返回价格信息]
    P --> Q[用户选择座位]
    Q --> R[跳转预订页面]
```

## 八、关注用户流程

```mermaid
graph TD
    A[用户进入他人主页] --> B[点击关注按钮]
    B --> C[调用followUser]
    C --> D[插入follows记录]
    D --> E[触发通知触发器]
    E --> F[生成follow类型通知]
    F --> G[插入notifications表]
    G --> H[被关注用户收到通知]
    H --> I[更新关注状态UI]
    I --> J[显示已关注状态]
```

## 九、实时通知流程

```mermaid
graph TD
    A[数据库插入通知] --> B[触发器执行]
    B --> C[插入notifications表]
    C --> D[Supabase Realtime推送]
    D --> E[前端接收WebSocket消息]
    E --> F[更新通知列表UI]
    F --> G[显示未读标记]
    G --> H[用户点击查看详情]
    H --> I[调用markAsRead]
    I --> J[更新通知为已读状态]
    J --> K[清除未读标记]
```

## 十、管理员后台流程

```mermaid
graph TD
    A[管理员登录] --> B[验证admin角色]
    B --> C{验证通过?}
    C -->|否| D[拒绝访问]
    C -->|是| E[进入管理后台Dashboard]
    E --> F[查看系统统计]
    F --> G[选择管理模块]
    G --> H{选择模块}
    H -->|用户管理| I[查看用户列表]
    H -->|评论管理| J[审核评论内容]
    H -->|知识库| K[管理AI知识库]
    H -->|API日志| L[查看API调用日志]
    I --> M[执行用户操作]
    M --> N[封禁/解禁用户]
    N --> O[记录操作日志]
    J --> P[删除/隐藏评论]
    P --> O
    K --> Q[添加/删除知识条目]
    Q --> O
    O --> F
```

## 十一、前端路由守卫流程

```mermaid
graph TD
    A[用户访问页面] --> B[检查路由meta信息]
    B --> C{需要登录?}
    C -->|否| D[直接放行]
    C -->|是| E[检查Session]
    E --> F{Session有效?}
    F -->|否| G[跳转登录页]
    F -->|是| H[检查用户状态]
    H --> I{用户被封禁?}
    I -->|是| J[登出并跳转登录]
    I -->|否| K{需要管理员?}
    K -->|是| L{是管理员?}
    L -->|否| M[跳转首页]
    L -->|是| D
    K -->|否| D
    G --> N[登录后跳转原页面]
```

## 十二、文件上传流程

```mermaid
graph TD
    A[用户选择文件] --> B[前端文件校验]
    B --> C{校验通过?}
    C -->|否| D[显示错误提示]
    C -->|是| E[压缩/优化图片]
    E --> F[生成唯一文件名]
    F --> G[调用supabase.storage.upload]
    G --> H[上传到Storage桶]
    H --> I{上传成功?}
    I -->|否| J[显示上传失败]
    I -->|是| K[获取文件公开URL]
    K --> L[返回URL给前端]
    L --> M[插入内容到数据库]
```

---

*文档生成时间：2026年3月*
*项目名称：基于Vue 3 + Supabase的云原生智慧旅游综合服务系统*
