## 2.4 后端Django开发框架

Django作为Python生态中最成熟的Web开发框架之一，以其"MTV"（Model-Template-View）的分层架构、强大的ORM系统与丰富的第三方扩展生态，为本系统的AI智能助手模块提供了稳定可靠的RAG应用开发基础。Django的设计理念强调"开箱即用"与"低耦合高内聚"，其自带的Admin后台、管理命令、表单系统等功能大幅提升了开发效率，而MTV模式与组合式API的结合则使得代码组织清晰、易于维护。在本系统中，Django主要用于构建RAG（检索增强生成）知识库服务，负责处理AI对话的向量检索、Prompt组装与大模型调用等核心逻辑，为前端Vue 3应用提供专业的AI能力支撑。

### 2.4.1 Django REST Framework与视图层设计

Django REST Framework（DRF）是构建Web API的利器，它在Django的基础上封装了丰富的视图类、序列化器与认证机制，极大简化了RESTful API的开发流程。本系统的RAG对话接口采用DRF的APIView作为视图基类，通过类方法区分不同的HTTP操作（GET/POST），使代码结构清晰且易于扩展。视图层的设计遵循单一职责原则，每个视图类专注于处理一类业务逻辑，如RagChatView负责处理对话请求，KnowledgeView负责管理知识库条目。DRF的请求解析（Request Parsing）与响应渲染（Response Rendering）机制自动处理JSON数据的序列化与反序列化，开发者在视图方法中可直接操作Python字典或模型实例，无需手动处理HTTP层面的细节。

视图层与前端的数据交互采用JSON格式，AI对话接口接收前端发送的JSON请求体，其中包含用户输入（user_input）、对话历史（messages）、模型参数（model）与提供商标识（provider_id）等字段。响应同样采用JSON格式，返回大模型的流式输出经由SSE（Server-Sent Events）协议推送至前端，前端通过EventSource API逐块解析并显示AI回复。DRF的SessionAuthentication与TokenAuthentication机制为API提供了可选的认证层，在本系统中通过Supabase颁发的JWT令牌进行身份验证，确保只有合法用户能够调用AI对话接口。

### 2.4.2 Django ORM与数据库交互

Django ORM（Object-Relational Mapping）系统提供了面向对象的数据库操作接口，将SQL查询封装为Python方法调用，大幅降低了数据库操作的复杂度。本系统的RAG模块需要与PostgreSQL数据库中的knowledge_base知识库表进行交互，Django ORM通过模型类（Model）的定义将数据库表映射为Python对象，开发者可使用filter()、exclude()、annotate()等链式方法构建复杂查询，无需编写原始SQL语句。knowledge_base表包含内容文本（content）、元数据（metadata）与向量嵌入（embedding）三个核心字段，其中向量字段通过pgvector扩展实现，Django ORM能够正确处理vector类型的序列化和反序列化，使其与Python数据结构之间的转换透明化。

Django ORM的查询优化机制对系统性能至关重要。通过select_related()与prefetch_related()方法可有效减少数据库查询次数，避免N+1查询问题。例如，在检索知识条目时使用only()方法指定返回字段，可减少不必要的数据传输；在需要按相关性排序时，使用order_by()方法配合Django的数据库函数实现高效排序。此外，Django的数据库迁移（Migration）机制使得数据库schema的版本管理变得简便，开发者可通过makemigrations与migrate命令创建并应用数据库变更，确保开发环境与生产环境的数据库结构保持一致。

### 2.4.3 Django中间件与请求处理

Django中间件（Middleware）是处理请求与响应的钩子框架，位于Web服务器与视图函数之间，可对请求进行预处理或对响应进行后处理。本系统的AI对话接口部署了自定义中间件，实现认证令牌验证、请求日志记录与异常处理等功能。认证中间件在请求进入视图之前解析Authorization头中的Bearer令牌，调用Supabase的JWT验证接口确认用户身份，将验证结果附加到request对象上供视图使用。日志中间件记录每个API请求的时间戳、用户ID、请求路径与响应状态码，为系统运维与问题排查提供数据支撑。异常处理中间件捕获视图中未处理的异常，统一返回格式化的错误响应，避免原始异常堆栈信息暴露给前端用户。

中间件的执行顺序遵循配置顺序，请求依次经过每个中间件的process_request方法，响应依次经过每个中间件的process_response方法。这种洋葱模型（Onion Model）的设计使得开发者可以灵活地在请求-响应链的任意位置插入自定义逻辑，如添加CORS头、压缩响应内容、统计接口调用次数等。在本系统中，Django CORS中间件配置允许来自前端应用的跨域请求，Security中间件添加安全响应头防止常见的Web攻击，确保API服务的安全性与可用性。

### 2.4.4 Django与RAG架构的结合

Django在本系统中承担RAG（Retrieval-Augmented Generation）架构的核心实现工作。RAG架构通过将信息检索与文本生成相结合，有效弥补了大语言模型在垂直领域知识方面的不足。本系统的RAG流程包含以下关键步骤：首先是知识检索步骤，当用户提出问题时，系统将问题文本转换为向量嵌入（Embedding），然后在knowledge_base知识库中执行相似度检索，找出与问题语义最相关的内容片段；其次是Prompt组装步骤，将检索到的知识片段与用户问题组装为结构化的Prompt，供大语言模型生成回答使用；最后是回答生成步骤，调用AI供应商的API（如智谱GLM、DeepSeek或讯飞星火）生成最终回答。

Django在RAG架构中的优势体现在多个方面。Django的视图层设计清晰分离了HTTP处理逻辑与业务逻辑，便于维护和测试；Django ORM提供了与PostgreSQL数据库的高效交互，支持向量检索与全文搜索的无缝结合；Django的异步任务队列（如Celery）支持长时间运行的AI请求异步处理，避免HTTP请求超时；Django的缓存框架（如Redis）可缓存高频查询的检索结果与AI响应，提升系统整体响应速度。通过这些能力的综合运用，Django为本系统构建了一个高效、稳定、可扩展的RAG服务层，与Vue 3前端和Supabase BaaS服务共同构成了完整的云原生智慧旅游系统架构。
