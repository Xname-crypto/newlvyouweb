# 第三章 智慧旅游综合服务系统需求分析与设计

## 3.1 系统总体需求分析

### 3.1.1 普通用户需求分析

普通用户是智慧旅游综合服务系统的主要服务对象，其功能模块包括：注册登录、首页浏览、社区互动、火车票查询、AI智能助手、社交互动。

注册登录：注册是用户进入系统的第一步，用户需在注册页面准确填写邮箱地址、密码等关键信息并完成确认操作，系统借助这些信息识别用户身份。注册成功后，用户凭借已注册的账号和对应密码，在登录页面进行输入操作后系统随即对用户输入的信息进行严格验证，只有当账号与密码完全匹配时，系统才会将用户引导至首页，用户方可在首页上展开后续的一系列操作。

首页浏览：用户登录成功后进入系统首页，首页采用全屏视差滚动设计，为用户提供沉浸式的浏览体验。页面展示了多个热门旅游目的地的内容区块，每个区块包含标题、描述文字与配图，用户可通过交替的左右布局浏览感兴趣的内容。系统还会在首页集成热门内容推荐区域，基于用户行为数据（如点赞数、收藏数、浏览量）综合计算内容热度，优先展示高质量的社区内容，帮助用户快速发现热门旅游景点与旅行攻略。

社区互动：社区是用户分享旅游经验、交流出行心得的核心平台。用户可在此浏览其他用户发布的游记内容，包括图文、视频、文章三种不同类型的帖子。当用户发现感兴趣的内容时，可通过点赞、收藏、评论等操作与其他用户进行互动。对于社区中的优质内容，用户还可以关注发帖作者，成为其粉丝以便获取该作者的最新动态。系统支持用户之间的一对一私信通信，方便用户进行更深层次的交流。

火车票查询：火车票查询模块为用户提供便捷的出行规划工具。用户输入出发地、目的地与出发日期后，系统调用12306接口返回可用车次列表，包括车次号、出发到达时间、历时、各座位类型余票数量等信息。余票数字的颜色会随紧缺程度变化：绿色表示充足、黄色表示紧张、红色表示仅剩少量。系统还支持学生票与婴儿票的特殊查询需求，并提供热门目的地排行榜，帮助用户发现热门旅游城市。

AI智能助手：AI助手是系统的核心亮点功能，用户可通过它获取个性化的旅游建议与行程规划。当用户提出旅游相关问题时，系统通过RAG架构检索私有知识库后生成回答，相比直接调用通用大模型更加准确可靠。系统支持多模型切换，用户可选择智谱GLM、DeepSeek或讯飞星火等不同模型进行对话。此外，AI助手还支持图片上传分析功能，用户发送风景图片并询问“这是哪里”，系统可准确识别图片中的地点。

社交互动：用户可对社区中的帖子和评论进行点赞与收藏操作，这些互动数据会影响内容在社区中的排序与曝光。系统允许用户关注其他用户，成为其粉丝后可在专门页面查看该用户的最新动态。用户之间还可以发送站内私信，进行一对一的信息交流。当用户收到评论回复、点赞通知、关注提醒时，系统会通过实时推送确保用户第一时间获知。

### 3.1.2 创作者用户需求分析

创作者用户是社区内容的主要贡献者，在普通用户功能基础上增加了内容管理与数据洞察能力。创作者功能模块包括：内容发布、内容管理、数据统计。

内容发布：创作者可通过发布功能在社区中分享自己的旅游经验和心得。系统支持图文、视频、文章三种内容类型的发布。当选择图文或视频类型时，创作者需先上传媒体文件至系统存储，获取文件URL后与标题、正文一并提交；当选择文章类型时，创作者可使用富文本编辑器编写内容，支持插入标题层级、列表、链接等元素。发布前系统会对内容进行敏感词检测与长度校验，通过后内容将正式发布并可见于社区列表。

内容管理：创作者可通过创作者中心模块查看自己发布的所有内容，包括已发布与草稿状态的帖子。对于已发布的帖子，创作者可随时编辑更新内容或将其下架；对于未完成的帖子，创作者可保存为草稿稍后继续编辑。系统还提供删除功能，允许创作者移除不再希望展示的内容。通过这些管理功能，创作者能够对自己的内容库进行全面的维护与更新。

数据统计：系统为创作者提供内容数据统计功能，帮助其了解每篇帖子的阅读量、点赞数、收藏数、评论数等关键指标。创作者可在创作者中心的数据看板中查看这些统计数据，通过分析数据变化趋势优化内容创作策略。此外，系统还提供粉丝列表与互动数据，创作者可查看自己的粉丝群体特征，了解受众群体的偏好特征，从而创作更符合粉丝期待的内容。

### 3.1.3 系统管理员需求分析

系统管理员负责平台的运营与治理工作，拥有最高级别的系统访问权限。管理员功能模块包括：登录、仪表盘概览、用户管理、人员管理、评论管理、知识库管理、数据源管理、API管理。

登录：登录是管理员进入后台管理系统的第一个步骤。当管理员在登录界面中输入用户名和密码时，系统会对输入的信息进行验证。当用户名和密码与系统数据库中的管理员账户相匹配时，管理员才能进入管理系统进行后续的运营管理工作。

仪表盘概览：管理员登录后进入仪表盘页面，该页面展示了系统的核心运营数据统计。管理员可以直观地看到总用户数、总帖子数、总评论数、今日活跃用户数等关键指标，以及这些指标的变化趋势。通过这些统计数据，管理员能够快速了解平台当前的运营状态，及时发现异常情况并采取相应的管理措施。

用户管理：针对系统内已注册的所有用户，管理员具备全面的管理权限。管理员能够查看用户列表，了解用户的注册时间、最后登录时间、账户状态等基本信息。对于普通用户，管理员可将其升级为创作者，赋予内容发布的权限；对于违规用户，管理员可将其列入黑名单进行封禁处理，被封禁的用户将无法登录系统。管理员还可查看用户的发帖记录、互动数据等，用于判断用户是否存在违规行为。

人员管理：人员管理用于管理系统内部的管理人员和审核员。管理员可以添加新的管理员或审核员账户，为不同人员分配相应的权限。审核员具有内容审核的权限但不具备其他管理功能，而管理员拥有完整的管理权限。管理员可以查看人员列表，了解每个账户的角色和状态，并可随时调整人员权限或禁用账户。

评论管理：管理员可对社区中的所有评论内容进行管理。系统会展示全站用户发布的所有评论，管理员可以按照时间、用户、内容等条件进行筛选查询。对于包含敏感词的评论，系统会自动标记提示管理员关注。管理员可对违规评论执行删除处理，删除后评论将显示为"[已删除]"状态。对于多次发布违规内容的用户，管理员可结合用户管理功能对其账户进行封禁。

知识库管理：AI智能助手依赖于知识库提供准确的旅游信息服务。管理员可通过知识库管理模块对AI知识库进行全面维护，包括查看现有的知识条目、添加新的知识内容、编辑已有知识的文本和元数据、删除过时或不准确的知识。系统支持知识条目的分类管理，管理员可以为知识设置标签便于检索。知识库管理还支持从外部URL抓取知识内容并自动生成知识切片，大大提高了知识录入的效率。通过持续的维护更新，确保AI助手能够提供准确、及时、专业的旅游信息服务。

数据源管理：数据源管理允许管理员管理系统所连接的各类数据库。管理员可以添加、编辑、删除数据源连接，配置数据库的连接参数。系统提供表结构查看功能，管理员可以查看各个数据表的字段信息。同时，管理员还可以执行数据预览，查看表中的实际数据记录。通过数据源管理功能，管理员能够对系统的数据资产进行有效的管理和监控。

API管理：API管理模块用于配置和管理AI服务供应商。系统支持多个AI供应商的接入，管理员可以添加智谱AI、DeepSeek、讯飞星火等不同供应商的API配置，包括API密钥、调用 endpoint、模型参数等。系统会统计各供应商的API调用情况，包括调用总数、今日调用量、Token消耗和平均响应时间等数据。当某个供应商出现问题时，管理员可以快速切换至备用供应商，保障服务的连续性。

### 3.1.4 系统非功能性需求

除却上述功能性需求外，系统的非功能性需求同样是需要重点考量的设计维度，直接影响用户体验与系统可靠性。

在性能需求方面，系统需保证良好的响应速度与并发处理能力。在响应时间方面，页面首次加载时间应控制在3秒以内，API接口的平均响应时间应不超过500毫秒，AI对话的首字响应时间应控制在1秒以内。在并发能力方面，系统需能够应对旅游旺季可能出现的高并发访问场景，依托Supabase云原生服务的自动伸缩特性，确保在突发流量情况下系统的稳定性与可用性。

在安全性需求方面，安全性是系统设计与实现的核心原则之一。在身份认证层面，系统采用Supabase内置的JWT（JSON Web Token）认证机制，所有API请求均需携带有效的身份令牌。在数据访问控制层面，数据库行级安全策略（Row Level Security，RLS）确保用户只能访问其有权查看的数据记录。在传输安全层面，全站强制启用HTTPS加密通信，防止敏感信息在传输过程中被窃取。此外，系统对用户输入内容进行严格的校验与过滤，防范SQL注入、跨站脚本等常见Web安全威胁。

在可用性需求方面，系统采用云原生架构设计，具备弹性伸缩能力与高可用特性。Supabase托管服务提供99.9%的SLA保障，数据库主从复制确保数据的高持久性。前端应用采用代码分割与懒加载策略，减少首屏加载时间的同时优化整体性能。实时数据订阅功能确保用户能够即时获取最新内容与互动反馈，提升用户体验的流畅性与实时性。

在可维护性需求方面，系统采用前后端分离架构，各模块职责边界清晰，代码组织遵循高内聚低耦合原则。前端基于Vue 3组合式API将同一逻辑关注点的代码组织在一起，提升了代码的可读性与可复用性。完善的类型定义（TypeScript）与组件化开发模式降低了大型项目的维护成本。Supabase BaaS模式使开发者无需管理服务器与数据库基础设施，显著降低了运维复杂度与成本。

## 3.2 系统架构设计

### 3.2.1 前后端分离架构设计

本系统采用经典的前后端分离架构模式，前端与后端通过定义良好的接口契约进行协作，各自承担独立的职责与角色。

在总体架构方面，系统整体采用B/S（Browser/Server）架构，前端为基于Vue 3的SPA（Single Page Application）单页应用，后端服务完全基于Supabase BaaS平台与Django RAG服务构建。前端层负责用户界面的渲染与交互逻辑的处理。基于Vue 3的响应式系统，前端能够高效地同步Supabase实时数据库的变更，为用户提供流畅的实时交互体验。Vue Router处理前端路由与页面导航，Pinia Store管理全局状态（如用户认证信息、AI会话缓存、主题配置等）。TailwindCSS提供原子化的样式方案，加速界面开发的同时确保设计风格的一致性。后端服务层由Supabase与Django RAG API两部分组成。Supabase作为BaaS平台，提供了身份认证（Auth）、实时数据库（Realtime Database）、对象存储（Storage）与边缘函数（Edge Functions）等核心服务。身份认证服务管理用户的注册、登录与会话管理；实时数据库基于PostgreSQL，支撑所有业务数据的CRUD操作；对象存储服务托管用户上传的图片、视频等媒体文件；边缘函数支持在靠近用户的边缘节点运行无服务器代码，用于处理图片生成、图像分析等计算密集型任务。Django RAG API专门处理AI智能助手相关的业务逻辑。RAG（Retrieval-Augmented Generation，检索增强生成）架构将向量检索与大规模语言模型相结合，为用户提供基于私有知识库的智能问答服务。

在前后端交互模式方面，前后端通过RESTful风格的API与WebSocket两种方式进行交互。数据查询与变更操作通过Supabase JS SDK发起，SDK内部封装了与Supabase后端服务的通信逻辑。对于AI对话等需要流式输出的场景，前端通过fetch API直接调用Django RAG接口，获取Server-Sent Events（SSE）格式的流式响应。实时性要求较高的场景（如评论更新、在线状态、通知推送）采用WebSocket通信。前端通过Supabase Realtime建立与数据库的实时订阅连接，当数据库发生变更时，变更数据通过WebSocket推送至前端，前端无需轮询即可即时更新界面。

### 3.2.2 数据库与存储设计

在关系型数据存储方面，系统核心业务数据存储于Supabase托管的PostgreSQL数据库中。PostgreSQL作为功能强大的开源关系型数据库，完美支持Supabase提供的行级安全策略、触发器、存储过程等高级特性。用户数据表（profiles）存储用户的基本信息，通过UUID类型的主键与Supabase身份认证系统（auth.users）关联。帖子数据表（posts）存储用户发布的游记内容，支持三种内容类型（图文、视频、文章），媒体文件URL以数组形式存储。评论数据表（comments）支持嵌套回复机制，通过parent_id自关联字段实现树形结构。互动数据表（interactions）记录用户对帖子或评论的点赞、收藏、分享、浏览等行为，采用唯一约束确保同一用户对同一内容同一类型的互动仅记录一次。关注数据表（follows）记录用户之间的关注关系，支持双向关注查询。

在向量数据库设计方面，AI智能助手的RAG架构依赖向量数据库存储文本嵌入（Embedding）。Supabase通过pgvector扩展在PostgreSQL中提供向量存储与检索能力。知识库表（knowledge_base）的embedding字段为vector(1024)类型，存储1024维的浮点数向量。HNSW（Hierarchical Navigable Small World）索引被应用于向量字段，支持高效的近似最近邻（ANN）检索，在检索精度与性能之间取得良好平衡。

在全文搜索设计方面，针对中文文本的搜索需求，系统集成PGroonga扩展实现全文搜索能力。相比PostgreSQL内置的全文搜索功能，PGroonga对中文分词的支持更加完善，检索效果更佳。knowledge_base表的content字段创建了PGroonga索引，支持基于关键词的全文检索。RAG架构采用混合检索策略，结合向量相似度检索与全文关键词检索的结果，通过RRF（Reciprocal Rank Fusion）算法进行融合排序，综合评估每条知识条目的相关性，最终选取Top-K条知识片段作为大模型生成的上下文输入。

在对象存储设计方面，用户上传的图片、视频等媒体文件存储于Supabase Storage服务中。系统定义了名为"media"的存储桶，存储桶策略配置为公开读取（经过身份认证的用户可获取上传文件的公开URL）。文件组织采用"用户ID/时间戳-文件名"的路径结构，确保文件命名的唯一性与可管理性。

## 3.3 系统核心功能模块设计

### 3.3.1 AI智能助手与RAG集成设计

AI智能助手是本系统的核心亮点功能之一，旨在为用户提供基于私有旅游知识库的智能问答服务。与直接调用通用大模型不同，本系统采用RAG架构将检索与生成相结合，有效缓解了大模型在垂直领域的"幻觉"问题，显著提升回答的准确性与时效性。

在RAG系统架构方面，RAG（Retrieval-Augmented Generation，检索增强生成）是一种将信息检索与文本生成相结合的技术架构。其核心思想是：在用户提出问题时，首先从私有知识库中检索与问题相关的知识片段，再将这些知识片段作为上下文输入给大语言模型，由模型基于检索到的真实信息生成回答。本系统的RAG架构包含以下核心组件：知识库层负责存储与管理旅游领域的专业知识，包括景点介绍、美食推荐、交通指南、行程规划等文本内容，每条知识条目关联元数据（如来源、标签、发布时间等）与预先计算好的向量嵌入；检索层负责接收用户Query，分别执行向量相似度检索与全文关键词检索，并通过RRF算法融合排序，返回最相关的知识片段；生成层负责将检索结果与用户问题组装为Prompt，调用大模型API生成最终回答。

在混合检索机制方面，为提升检索效果，系统采用向量检索与关键词检索相结合的混合检索机制。向量检索利用文本嵌入模型将知识库内容与用户问题转换为高维向量，通过计算向量间的余弦相似度找出语义上最相关的内容。这种方式能够捕捉语义相似性，例如用户问"带老人去哪里旅游比较好"，系统能够检索到"适合老年人的休闲景点推荐"等语义相关的知识。关键词检索利用PGroonga全文索引匹配用户问题中的关键词，能够精准命中用户明确表达的概念与实体。例如用户提到"张家界"，系统会直接检索包含该关键词的知识条目。RRF（Reciprocal Rank Fusion）算法对两种检索方式的结果进行融合排序。设某知识条目在向量检索中排名第一，在关键词检索中排名第二，则其RRF综合得分较高。通过这种融合方式，系统能够兼顾检索结果的语义相关性与关键词精确匹配，输出更符合用户期望的答案。

在多模型切换机制方面，为提供更灵活的AI服务选择，系统支持多模型切换机制。当前系统集成了三个主流大模型供应商：智谱AI的GLM-4模型、深度求索的DeepSeek-chat模型以及讯飞星火模型。模型切换逻辑通过provider_id参数控制，用户可在界面上选择偏好的模型。系统设计了模型路由抽象层，屏蔽不同模型API的差异性，提供统一的调用接口。当某个模型供应商出现服务异常时，系统可快速切换至备用模型，保障服务的连续性。此外，系统支持"联网搜索"开关。开启时，RAG架构不仅检索本地知识库，还会调用搜索工具获取互联网上的实时信息，进一步增强回答的时效性与覆盖面。

在会话管理机制方面，AI助手支持多会话管理，每个用户可创建多个独立的话题会话。会话数据存储于assistant_sessions与assistant_messages表中，包括会话ID、标题、创建时间、消息列表等信息。用户在新建对话时可自定义会话标题，便于后续查找与回顾历史对话。系统记录完整的对话上下文，每次交互时将历史消息列表发送至RAG API，支持多轮对话场景。大模型在生成回答时能够理解对话的连贯性，提供更精准的follow-up服务。

### 3.3.2 高互动社交社区与实时票务模块设计

社交社区是用户分享旅游经验、交流出行心得的核心平台。社区模块支持图文、视频、文章三种内容类型的发布，满足不同创作场景的需求。

在社交社区模块设计方面，内容发布流程设计如下：用户在发布页面选择内容类型，若选择图文或视频则需先上传媒体文件至Supabase Storage，获取文件URL后与标题、正文一并提交至posts表；若选择文章类型，则使用Tiptap富文本编辑器编写内容，支持插入图片、链接等元素。发布前系统对内容进行敏感词检测与长度校验，通过后即正式发布并可见于社区列表。评论系统支持嵌套回复机制，用户可对帖子进行一级评论，也可回复他人的评论形成二级乃至多级回复树。评论数据存储于comments表，通过parent_id自关联字段实现树形结构。UI层面采用缩进与连线的方式呈现评论层级关系，为用户提供直观的对话流阅读体验。互动系统记录用户对内容的各种行为，包括点赞、收藏、分享与浏览。其中点赞与收藏是最核心的互动指标，影响内容在社区中的排序与曝光。互动数据存储于interactions表，post_id与comment_id双外键设计支持对帖子与评论的互动记录。

在实时订阅机制设计方面，为提供流畅的实时交互体验，系统充分利用Supabase的Realtime实时订阅功能。数据库变更订阅是最基础的实时能力。前端对posts、comments、interactions、notifications等核心数据表建立实时订阅，当这些表发生INSERT、UPDATE、DELETE操作时，变更数据通过WebSocket推送至订阅的前端客户端。前端根据变更类型更新相应的UI状态，如评论列表自动追加新评论、点赞数实时递增等。在线状态感知是另一项重要的实时功能。系统利用Supabase Presence特性追踪用户的在线状态。用户打开应用时加入presence频道，系统记录其在线信息；用户关闭页面或切换至后台时，Presence自动清理该用户的在线状态。其他用户可在社区中看到好友的在线状态标识，增强社交互动的即时感。通知推送机制与数据库触发器紧密配合。当用户收到点赞、评论、关注等事件时，数据库触发器自动生成通知记录，同时通过实时订阅将通知推送至接收者前端，实现通知的秒级送达。

在实时票务模块设计方面，火车票查询模块为用户提供便捷的出行规划工具，整合12306官方数据源，提供实时、准确的票务信息服务。模块支持单程与往返两种查询模式。用户输入出发地、目的地与出发日期（或往返日期）后，系统调用12306接口获取可用车次列表。车次信息包括车次号、出发/到达时间、历时、出发地/到达地、座位类型与余票数量等。系统设计了座位类型的动态价格查询功能。用户将鼠标悬停于某座位类型上时，前端触发价格详情请求，获取该车次该座位类型的详细票价信息。这一设计在保证页面快速加载的同时，按需加载价格数据，优化用户体验。票务模块还支持学生票与婴儿票的特殊查询需求，适配学生群体与携带婴幼儿出行的家庭用户。热门目的地排行榜展示近期用户搜索频率最高的城市，帮助用户发现热门旅游目的地。

## 本章小结

本章完成了智慧旅游综合服务系统的需求分析与架构设计工作。首先，通过用户角色划分与核心用例分析，明确了系统的功能性需求，涵盖普通用户、创作者用户与系统管理员三类角色的差异化需求。其次，从性能、安全性、可用性与可维护性四个维度阐述了系统的非功能性需求。在此基础上，详细设计了前后端分离的系统架构，明确了前端Vue 3应用、Supabase BaaS服务与Django RAG API的协作关系。最后，针对AI智能助手与RAG集成、高互动社交社区与实时票务两个核心功能模块进行了深入的方案设计，为后续的系统实现奠定了基础。
