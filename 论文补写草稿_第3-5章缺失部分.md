# 第3章至第5章缺失部分补写草稿

## 3.2.3 数据库与存储设计

本系统的数据层采用 Supabase 提供的 PostgreSQL 数据库与 Storage 对象存储服务实现。数据库承担用户身份信息、社区内容、互动行为、通知消息、票务订单及 AI 会话等结构化数据的持久化任务；对象存储主要用于用户头像、社区帖子图片、AI 上传图片等非结构化媒体资源的保存与分发。相较于传统“数据库 + 独立文件服务器”的分离式部署方式，Supabase 在统一平台内同时提供关系型数据库、鉴权、对象存储与实时订阅能力，使系统能够在较少运维成本下完成业务闭环。

从核心数据实体来看，系统以 `profiles` 表作为用户中心表，字段包括用户唯一标识、用户名、头像、个人简介及更新时间等信息，其中主键 `id` 与 `auth.users` 保持一一映射关系。为保证注册后业务资料自动同步，系统通过 `handle_new_user` 触发器在 `auth.users` 插入后自动生成对应的 `profiles` 记录，从而实现认证域与业务域的无缝衔接。围绕社区业务，系统设计了 `posts`、`comments`、`interactions`、`follows`、`follows_history` 与 `notifications` 等多张表。其中，`posts` 用于存储文章、图片、视频等内容；`comments` 通过 `parent_id` 实现评论树结构；`interactions` 记录点赞、收藏、分享、浏览等行为；`follows` 保存关注关系；`follows_history` 通过触发器自动记录关注和取关事件；`notifications` 则统一管理评论、点赞、收藏和关注等通知类型，构成完整的社交业务数据链路。

在票务模块中，系统额外设计了 `trains`、`train_stations` 与 `train_orders` 三类数据表。`train_stations` 用于维护车站名称与站点代码映射关系，为前端输入城市名称后调用 12306 接口提供统一转换依据；`train_orders` 用于保存用户订单信息，包括车次、出发地、目的地、乘车日期、座席类型、乘车人信息、订单状态及价格等内容。由于本系统现阶段以查询与展示为主，票务数据更多通过边缘函数实时拉取，但订单结构已预留完整字段，便于后续扩展支付和退改签能力。

在 AI 知识服务方面，系统同时使用了两类知识存储结构。其一是 Supabase 中的 `knowledge_base` 向量知识表，配合 `vector` 扩展、HNSW 索引与 PGroonga 中文检索索引，实现向量检索与关键词检索的混合召回；其二是 Django 本地侧维护的 Chroma 向量库，用于加载已构建的旅游知识数据集，支撑当前前端实际接入的 `/api/rag/chat/` 检索增强问答流程。为了保证管理可控性，后端还定义了 `EmbeddingProfile`、`KnowledgeBase`、`AdminQATestSession` 与 `AdminQATestMessage` 等模型，用于知识库配置、资料集拆分、管理员问答测试和发布审批。这种“云端业务数据库 + 本地向量检索引擎”的组合方案兼顾了业务数据统一管理与知识检索灵活扩展两个目标。

在存储设计方面，系统通过 Supabase Storage 创建 `media` 桶统一存储图片和媒体资源，并通过公开读策略与认证上传策略实现安全控制。前端发布模块与 AI 助手模块均通过 `supabase.storage.from('media').upload()` 完成文件上传，再通过 `getPublicUrl()` 生成可访问地址，将资源 URL 写回数据库中的 `media_urls`、`avatar_url` 或聊天消息图片字段中。该设计使数据库只保留资源引用而不直接存储二进制内容，减轻了关系型数据库压力，提高了媒体分发效率。

为保证数据安全，系统对 `profiles`、`posts`、`comments`、`interactions`、`messages`、`train_orders`、`follows`、`notifications` 等核心表全面启用了行级安全策略。公开数据如个人主页、帖子、评论和互动统计允许匿名读取；新增、修改、删除等写操作则严格限定为数据拥有者本人或管理员角色执行。例如，帖子仅允许 `auth.uid() = user_id` 的用户插入，评论仅允许评论创建者删除，私信仅允许发送方与接收方读取。这种基于 RLS 的细粒度权限控制，与前端路由守卫和后端 Token 校验共同构成了系统的数据访问安全体系。

总体来看，本系统数据库与存储设计遵循“结构化数据关系建模、非结构化资源对象存储、权限控制前后端协同、知识数据检索增强”的设计原则，既满足了智慧旅游社区平台的日常业务需求，也为 AI 服务、实时互动和管理后台扩展预留了充足空间。

## 3.3 系统核心功能模块设计

结合系统实际实现情况，平台核心功能模块可划分为智能旅游助手模块、高互动社区模块、实时票务模块、用户与权限管理模块以及后台知识管理模块五个部分。各模块并非孤立运行，而是围绕 Supabase 提供的数据底座形成统一业务闭环。用户层通过前端页面完成浏览、提问、发帖、评论、关注与车票查询等操作；数据层负责存储用户画像、社区内容、通知消息和订单信息；AI 服务层则调用 Django RAG 接口或边缘函数完成内容生成、知识检索和图像分析；管理层通过后台完成知识库维护、数据源导入、API 提供商切换与问答测试审批。

其中，智能旅游助手模块承担知识问答、图像理解与部分联网扩展能力，是系统差异化服务的核心；高互动社区模块负责帖子发布、评论回复、点赞收藏、粉丝关注与通知回流，是系统维持用户活跃度的主要抓手；实时票务模块聚焦火车票车次查询、座席余票展示与价格查询，是旅游决策场景中的即时服务能力；用户与权限模块负责注册登录、身份校验、角色控制、封禁监听与资料管理；后台知识管理模块则服务于管理员，通过知识库容器、资料集、文档导入、问答测试与发布审批保障 AI 答案质量。上述模块之间通过统一用户身份、统一数据接口和统一消息流完成协同，从而形成面向游客、创作者与管理员三类角色的综合服务平台。

### 3.3.1 AI智能助手与RAG集成设计

AI 智能助手模块采用“前端会话管理 + Django RAG 接口 + 知识库检索 + 多模型路由”的分层结构设计。前端 `Assistant.vue` 页面负责输入采集、消息展示、语音输入、图片上传、模型选择和会话切换等交互逻辑；`assistantService.ts` 负责会话查询、消息持久化、图片上传、图片分析及聊天请求发送；后端 `/api/rag/chat/` 接口则负责用户鉴权、知识库状态校验、检索增强问答执行以及调用日志记录。当前项目实际运行时，文本问答优先调用 Django 后端的 RAG 接口，而不是直接走 Supabase Edge Function `chat`，这是本系统实现中的一个关键技术事实。

从会话组织方式看，系统通过 `assistant_sessions` 与 `assistant_messages` 两类数据结构保存用户历史会话和消息记录。用户首次提问时，前端会自动创建会话，并以首轮提问内容生成会话标题；随后用户消息与助手回复均写入消息表中，同时更新会话的 `updated_at` 时间，用于实现历史会话列表按最近使用时间排序。这种设计既方便用户回溯对话上下文，也为后续的个性化偏好建模提供了基础数据来源。

在 RAG 设计方面，系统采用“知识准入控制 + 检索增强生成 + 管理员测试审批”的方案。管理员先在后台创建知识库容器和资料集，再通过本地文件上传、网页爬取或旧数据导入等方式补充文档内容；随后后台可针对指定知识库发起管理员问答测试，测试结果保存在 `AdminQATestSession` 与 `AdminQATestMessage` 中。只有当知识库状态被审批为 `active` 时，前端普通用户才可调用该知识库进行问答。`rag_chat` 接口内部会首先检查知识库状态，若知识库处于 `pending_review` 或系统尚无已发布知识库，则直接返回禁止使用提示信息。该设计避免了未经验证知识直接向用户暴露，提升了答案质量控制能力。

在检索实现层面，系统同时保留两类 RAG 路径。第一类是 Supabase 侧 `knowledge_base` 表的混合检索方案：使用 `vector(1024)` 字段存储嵌入向量，借助 HNSW 索引提升向量近邻搜索效率，并使用 PGroonga 为中文文本建立全文检索索引；在查询阶段通过 `match_knowledge_base` 函数将向量相似度结果与关键词检索结果基于 RRF（Reciprocal Rank Fusion）进行加权融合，以兼顾语义相关性和关键词命中精度。第二类是 Django 本地的 Chroma 向量库方案：`rag_service.py` 在服务启动时检查本地 `chroma_db_knowledge` 目录，若存在预构建向量库则直接连接，否则可通过管理命令和脚本重新构建。当前前端用户端实际走的是 Django 路径，因此论文中应说明系统在演进中形成了“云端检索方案预留 + 本地向量库先行落地”的双轨结构。

在模型调用层面，系统通过 `api_providers` 配置表与 Django `llm_router.py` 实现了多提供商路由机制。管理员可以在后台配置不同的聊天或图像模型提供商，包括智谱、DeepSeek、OpenAI 兼容接口、讯飞星火或本地 Ollama 服务，并设置优先级、基础地址、模型标识和 API Key。前端助手页面在加载时通过 `/api/api-providers/public/` 接口获取当前可用模型列表，用户选择模型后，后端根据 `provider_id` 和 `model` 参数动态解析目标大模型并完成降级回退。该方案降低了系统对单一大模型平台的依赖，提高了 AI 服务的可替换性和可运维性。

此外，助手模块还融合了图像上传与图像理解功能。用户可在对话框中上传图片，前端先将图片写入 Supabase Storage，再调用 `analyze-image` 边缘函数进行视觉内容分析；若用户输入文本中包含明显绘图意图，则可转而调用 `generate-image` 边缘函数生成图像内容。由此，系统智能助手不仅支持文本知识问答，还可覆盖“图像理解”和“文生图”两类多模态场景，增强了旅游攻略创作与行程辅助的交互深度。

### 3.3.2 高互动社交社区与实时票务模块设计

社区模块采用“内容发布、互动反馈、关系连接、通知回流”的分层结构设计。内容发布层以 `posts` 表为核心，支持文章、图片和视频三种内容类型；前端发布服务会先将图片或视频上传至 `media` 存储桶，再将返回的资源地址写入 `media_urls` 字段，实现媒体资源与内容记录的解耦。互动反馈层通过 `comments` 和 `interactions` 两张表完成评论、回复、点赞、收藏、分享等行为建模，其中评论表通过 `parent_id` 实现多层回复，互动表则使用唯一约束避免用户对同一对象重复执行同类操作。关系连接层通过 `follows` 与 `follows_history` 管理关注关系和历史变更记录。通知回流层借助 `notifications` 表以及评论、互动、关注触发器，在行为发生后自动为被动接收方生成通知，形成闭环的信息反馈机制。

为了提升社区的交互实时性，系统在前端使用 Supabase Realtime 建立了多种订阅通道。一方面，`usePresence.ts` 通过 `supabase.channel('online-users')` 与 Presence 机制追踪用户在线状态，维护当前在线用户集合；另一方面，主应用 `App.vue` 中还为当前登录用户建立 `profiles` 表更新订阅，用于在用户被管理员封禁时实时触发退出登录逻辑。此外，通知页面与评论页面通过查询 + 增量刷新方式及时呈现最新评论、点赞和关注变化。该设计说明系统并非只依赖传统轮询，而是充分利用 Supabase 的实时推送特性，增强了社区系统的即时反馈能力。

在社区推荐设计上，系统采用了轻量级内容分类与兴趣匹配策略。`Community.vue` 会根据帖子标题和正文中的关键词，将内容自动归类为景点、路线、美食、酒店、攻略、火车等类别；同时读取用户 `profiles.bio` 中保存的兴趣信息，将兴趣标签与帖子语义进行简单打分匹配，并对推荐页进行排序。虽然这一策略尚未引入复杂的深度推荐模型，但其实现成本较低、可解释性较强，适合作为毕业设计阶段的工程化实现方案。

实时票务模块则采用“前端条件输入 + Supabase 边缘函数代理 12306 接口 + 本地结果筛选展示”的技术路线。`TrainTickets.vue` 页面负责采集出发地、目的地、日期、往返类型、学生票、座席过滤等查询条件，并通过城市名称到站点代码映射工具将用户输入转换为 12306 接口可识别的站码。用户发起查询后，前端调用 Supabase Edge Function `query-tickets`，由边缘函数在服务端发起对 12306 余票接口的请求，解析返回字符串中的车次、时间、历时和各类座席余票数量，再将结构化数据返回前端展示。对于价格信息，前端采用“点击座席后按需查询”的方式，再调用 `query-price` 边缘函数请求 12306 价格接口，从而避免初始列表渲染时产生过多外部请求。

从设计角度看，票务模块引入 Supabase Edge Function 的核心价值在于解决跨域访问和第三方接口头部伪装问题。由于 12306 官方接口对浏览器直连支持较差，且通常要求特定的 User-Agent、Referer 与 Cookie 头，若直接由前端访问将面临明显的跨域与风控障碍。通过将请求转移至边缘函数执行，系统既可以统一控制请求头，又能在接口失败或返回非 JSON 时优雅降级为模拟数据或错误提示，提高了用户体验的稳定性。

综上，社区模块负责沉淀用户生成内容与社交关系，票务模块负责提供即时出行服务，两者共同支撑了“内容种草 + 行程决策 + 实时服务”的智慧旅游平台核心价值链。

## 4 智慧旅游综合服务系统的实现

### 4.1 开发环境与项目初始化

本系统采用前后端分离的实现方式。前端使用 Vue 3、TypeScript 与 Vite 构建单页应用，负责页面渲染、交互逻辑与 Supabase SDK 调用；后端采用 Django + Django REST Framework 实现管理接口、RAG 问答接口和知识库管理逻辑；数据平台由 Supabase 提供 PostgreSQL、Auth、Storage、Realtime 与 Edge Functions 能力。该组合既保持了前端开发效率，也便于将 AI 检索与后台运维能力集中在 Python 生态中实现。

在开发工具方面，项目通过 Node.js 与 npm 管理前端依赖，通过 Python 虚拟环境管理 Django 后端依赖，并通过 Supabase CLI 与 Dashboard 管理边缘函数和数据库脚本。前端与后端在本地开发时分别运行于 Vite 开发服务器与 Django 服务进程，前端再借助 Vite 代理将 `/api` 请求转发到 Django 服务，将 `/functions/v1` 请求代理到 Supabase 项目地址，从而在开发环境中屏蔽跨域复杂度，提升联调效率。

#### 4.1.1 前端项目工程化配置

前端项目通过 `npm create vite` 初始化，采用 Vue 3 + TypeScript 模板作为基础工程。`package.json` 中配置了 `dev`、`build`、`preview`、`check`、`lint` 与 `lint:fix` 等脚本，分别用于本地开发、生产构建、预览、类型检查和静态代码校验。依赖层面，系统引入 `vue-router` 实现页面路由、`@supabase/supabase-js` 实现云服务接入、`@tiptap/vue-3` 实现富文本编辑、`vue-cropper` 实现图片裁剪、`lucide-vue-next` 实现图标渲染，并通过 Tailwind CSS、PostCSS 与 Autoprefixer 完成原子化样式体系构建。

在工程配置上，`vite.config.ts` 完成了多个关键能力的统一配置。首先，项目通过 `resolve.alias` 将 `@` 映射到 `src` 目录，简化了组件和服务模块的导入路径。其次，开发服务器中配置了 `/api` 到 `http://127.0.0.1:8000` 的反向代理，用于对接 Django 后端；同时配置 `/functions/v1` 到 Supabase 项目域名的代理规则，用于在本地环境下透明调用边缘函数。再次，构建阶段开启了隐藏式 Source Map，兼顾线上问题排查与源码保护。最终形成了“组件模块化、路径别名化、接口代理化、构建标准化”的前端工程结构。

从目录组织上看，`src` 目录按照“视图层、组件层、服务层、组合式逻辑层、工具层、数据库脚本层”进行划分。`views` 存放页面级组件，如首页、社区、助手、个人中心、通知页和票务页；`components` 存放可复用界面模块，如导航栏、评论卡片、社区瀑布流和富文本编辑器；`services` 负责封装对 Supabase 和后端接口的调用逻辑；`composables` 存放 `useToast`、`usePresence`、`useTheme` 等跨页面可复用状态逻辑。该分层方式提升了前端代码的可维护性和后期扩展能力。

#### 4.1.2 Supabase服务连接与初始化

Supabase 的前端连接封装在 `src/utils/supabase.ts` 中。系统从环境变量 `VITE_SUPABASE_URL` 与 `VITE_SUPABASE_ANON_KEY` 读取项目地址与匿名公钥，并通过 `createClient()` 生成全局 Supabase 客户端实例，供认证、数据库、对象存储和实时订阅统一复用。当环境变量缺失时，系统会在控制台输出提示信息，以便开发阶段快速定位配置问题。

在数据库初始化方面，项目将结构化 SQL 脚本集中放置于 `src/db` 目录中，包括基础表结构 `schema.sql`、知识检索结构 `rag_setup.sql`、评论与互动权限脚本、关注历史脚本、通知触发器脚本、票务表脚本以及对象存储修复脚本等。系统首先执行基础表结构脚本，完成 `profiles`、`posts`、`comments`、`messages`、`interactions` 等表的创建；再执行 `fix_storage_and_profiles.sql` 创建 `media` 存储桶并补齐已有用户资料；最后根据业务需求增量执行关注、通知、票务与 RAG 等补充脚本，从而实现数据库的分阶段演进。

在认证与会话管理方面，Supabase Auth 负责用户注册、登录、退出及会话续期。前端通过 `supabase.auth.getSession()` 和 `supabase.auth.onAuthStateChange()` 获取并监听当前会话状态；后端 Django 则通过自定义认证中间件解析前端携带的 Supabase JWT，并在需要时调用 `/auth/v1/user` 或 `profiles` 表校验用户角色。该机制保证了前后端在不同技术栈下仍能共享同一身份体系。

在对象存储初始化方面，系统统一创建 `media` 桶并配置公开读取策略与认证上传策略。社区内容图片、用户头像以及 AI 上传图片均复用该桶管理。相比在多个桶中分散存储，该做法简化了前端上传逻辑和数据库字段设计，更适合中小型毕业设计项目的工程实现。

### 4.2 前端交互界面的实现

前端界面实现遵循“页面负责编排、组件负责展示、服务负责数据、组合函数负责共享状态”的原则。首页、社区页、AI 助手页、通知页、票务页与后台页均采用 Vue 3 单文件组件实现，并通过 `<script setup>` 语法组织逻辑，使模板、状态与事件处理在同一文件中保持清晰对应关系。整体界面采用 Tailwind CSS 实现响应式布局、间距系统和交互状态样式，从而在较少手写 CSS 的情况下完成页面风格统一。

#### 4.2.1 基于组合式API的页面组件实现

系统前端以组合式 API 作为主要开发方式。各页面使用 `ref`、`computed`、`onMounted`、`onUnmounted`、`watch` 等 API 组织状态和生命周期逻辑，避免了选项式 API 中数据、方法和生命周期分散声明的问题。例如，AI 助手页使用 `ref` 管理输入内容、消息列表、当前会话、模型菜单状态和提交状态，通过 `onMounted` 初始化模型列表，通过 `nextTick` 控制消息滚动到底部；社区页使用 `computed` 对帖子列表进行分类过滤与个性化推荐排序；票务页则通过多个 `ref` 管理站点、日期、筛选条件、车票列表与加载状态，实现单页内复杂查询交互。

在组件封装层面，系统将大量可复用 UI 元素拆分为独立组件。例如，社区模块中的 `CommunityMasonry.vue` 负责瀑布流内容布局，`PostModal.vue` 负责帖子详情展示，`PostCard.vue` 负责单个帖子卡片渲染；AI 助手模块中的 `HistorySidebar.vue` 负责历史会话切换，`ImageLoading.vue` 负责文本和图像生成中的加载态展示；发布模块中的 `ArticleEditor.vue` 与 `RichTextEditor.vue` 则基于 Tiptap 封装了富文本写作能力。通过这种页面与组件分层设计，系统界面既能保持一致的视觉风格，又能提高代码复用率。

在富交互实现上，系统还结合了浏览器原生能力和组合式逻辑。AI 助手页面集成了语音输入功能，通过 `SpeechRecognition` 接口将语音内容转写为文本；文件上传功能则通过隐藏式 `<input type="file">` 与预览 URL 的组合实现即时图片预览；通知页与个人中心借助统一的 Toast 机制反馈操作结果；App 根组件还通过实时监听实现“被封禁后自动退出”的交互效果，增强了系统响应性。

#### 4.2.2 状态管理与路由导航实现

本系统在当前实现中并未引入 Pinia，而是采用“组合式 API + 可复用 composables + 局部响应式状态”的轻量化状态管理方案。对于跨组件共享但体量较小的状态，如消息提示、主题状态和在线用户状态，系统分别通过 `useToast`、`useTheme` 与 `usePresence` 进行封装。`useToast` 通过模块级 `ref` 维护全局提示状态，实现任意页面统一弹出成功、失败、警告和信息提示；`useTheme` 将主题偏好持久化到 `localStorage` 并同步到 `document.documentElement`；`usePresence` 则借助 Supabase Presence 通道维护当前在线用户集合。该实现方式在项目规模尚可控的前提下，降低了额外状态库的接入成本。

页面导航则由 Vue Router 统一管理。`src/router/index.ts` 中定义了前台页面、用户页面、创作者页面和后台管理页面等多组路由，并为需要登录的页面设置 `meta.requiresAuth`，为后台页面设置 `meta.requiresAdmin`。在全局前置守卫中，系统首先通过 `supabase.auth.getSession()` 判断用户是否已登录，再查询 `profiles.role` 判断是否具备管理员或版主权限；若普通用户访问后台页面，则直接重定向到首页；若未登录用户访问受保护页面，则跳转到登录页。与此同时，路由还配置了平滑滚动行为，使页面切换时能自动滚动至顶部或恢复历史滚动位置，优化了单页应用的导航体验。

需要指出的是，虽然论文小节名称可写为“状态管理与路由导航实现”，但从真实项目实现角度，应明确说明系统采用的是组合式共享状态模式，而非典型的 Pinia 集中式 Store。这种表述更符合项目现状，也更容易得到指导老师对实现真实性的认可。

### 4.3 后端业务逻辑与数据交互实现

系统后端业务逻辑主要由 Supabase 云服务与 Django 后端共同承担。Supabase 负责数据库读写、鉴权、对象存储、实时订阅及边缘函数执行；Django 负责 RAG 问答服务、知识库管理、API 提供商配置、管理员测试工作流以及对 Supabase JWT 的二次校验。前端服务层通过两类接口访问后端：一类是 `@supabase/supabase-js` 直接连接 Supabase 数据库和存储；另一类是通过 `/api/*` 路径访问 Django REST 接口。这样的双后端协同结构使系统既能享受 BaaS 的高开发效率，又能在 AI 逻辑复杂场景中保持足够的可控性。

#### 4.3.1 数据库操作与实时订阅实现

前端对数据库的访问以服务层封装为主。以社区帖子为例，`publishService.ts` 通过 `supabase.from('posts')` 完成内容新增、更新、删除与查询，并在查询时联表拉取 `profiles` 中的用户名和头像信息；评论服务 `commentService.ts` 通过 `comments` 表完成评论列表加载、评论发布与软删除，同时结合 `interactions` 表统计评论点赞数；互动服务 `interactionService.ts` 则在点赞和收藏操作前先检测记录是否存在，再执行插入或删除，以实现前端按钮的“切换式交互”；个人资料服务 `profileService.ts` 负责用户资料修改、统计数据聚合和关注关系统计。通过服务层统一封装，页面组件不需要直接处理复杂查询细节，从而降低了视图层耦合度。

在实时订阅实现方面，系统重点使用了 Supabase Realtime 的 `postgres_changes` 与 `presence` 两类能力。`App.vue` 中为当前登录用户订阅了 `profiles` 表更新事件，一旦检测到当前用户角色被改为 `banned`，系统会立刻弹出提示、执行登出并跳转到登录页面，从而实现账号封禁的实时生效。`usePresence.ts` 则通过在线用户通道追踪 Presence 状态，在频道订阅成功后执行 `track()` 上报当前用户在线时间，并在同步事件中重建在线用户集合。该设计证明系统在业务关键状态变化场景下已实现近实时反馈，而非传统的延迟轮询模式。

通知模块同样体现了数据库操作与事件驱动结合的思路。数据库层面，评论、点赞、收藏和关注行为发生时，会通过数据库触发器自动在 `notifications` 表生成对应通知记录；应用层面，通知页面进入时根据当前标签批量将未读记录标记为已读，再分别调用对应的服务函数查询点赞收藏、评论回复和新增关注信息。这样既减轻了前端对多业务表联动的负担，也将“通知生成”从页面逻辑下沉到数据库逻辑，增强了系统的一致性。

#### 4.3.2 身份认证与边缘函数业务逻辑

用户认证由 Supabase Auth 提供基础能力。前端注册和登录页面通过 `supabase.auth.signUp()`、`signInWithPassword()` 和 `signOut()` 完成账号生命周期管理，并将会话令牌自动保存在浏览器本地。用户登录后，系统通过 `on_auth_user_created` 触发器自动补齐 `profiles` 表资料，实现身份域到业务域的扩展。路由守卫负责在进入页面前验证是否存在合法会话，并结合 `profiles.role` 控制后台权限和封禁状态。

在 Django 侧，系统通过 `auth_middleware.py` 对前端携带的 Supabase JWT 进行二次校验。中间件会优先调用 Supabase Auth 接口获取用户信息，若失败则使用 JWT 本地解码方式验证 Token 是否过期、是否由合法密钥签发，并进一步从 `profiles` 表中查询用户角色，用于判断当前请求是否具备管理员或版主权限。这种后端校验机制确保了前端无法单纯依赖界面控制绕过权限验证。

边缘函数主要承担第三方服务代理与多模态能力扩展任务。`query-tickets` 函数封装了对 12306 余票接口的请求与结果解析逻辑，在面对非 JSON 返回或接口风控时提供空结果或模拟数据降级；`query-price` 函数按需请求 12306 票价接口，供前端悬浮或点击座席时展示价格；`generate-image` 和 `analyze-image` 函数则分别用于 AI 图像生成与图像内容理解；`amap-proxy` 负责天气、地理编码和路线规划等地图相关服务转发。由于这些能力通常涉及跨域限制、第三方密钥保护或请求头伪装，因此放置于边缘函数层执行更为合理。

需要特别说明的是，聊天边缘函数 `supabase/functions/chat` 在仓库中仍然保留，内部包含基于向量检索、联网搜索、工具调用和多模型接入的完整实现逻辑，但前端当前真实调用入口已经切换为 Django 的 `/api/rag/chat/`。因此，在论文实现描述中应将 Supabase `chat` 函数定位为系统在 AI 服务方向上的保留方案与可扩展实现，而将 Django RAG 接口明确写为当前线上主路径。

## 5 系统测试与性能分析

### 5.1 测试环境与策略

为了验证智慧旅游综合服务系统在真实使用场景下的可用性与稳定性，本文从功能测试、非功能测试与性能评估三个层面构建测试体系。测试对象覆盖用户注册登录、社区发帖互动、AI 助手问答、通知回流、票务查询以及后台知识库管理等关键业务流程，测试目标包括功能正确性、交互完整性、权限安全性、响应效率与并发承载能力。

从测试环境上看，系统前端运行于基于 Node.js 的 Vite 开发与构建环境，后端运行于 Django 服务环境，数据库、存储与实时服务由 Supabase 承担。根据项目现有测试记录，可将测试环境描述为：客户端操作系统采用 Windows 11，开发工具使用 VS Code，前端运行环境为 Node.js 18.x 与 npm 9.x，后端为 Python 虚拟环境与 Django REST Framework，云端能力依赖 Supabase 服务，AI 相关接口依赖 Django RAG 服务与多模型提供商配置。

#### 5.1.1 功能测试用例设计与执行

功能测试采用黑盒测试与场景驱动测试相结合的方法。测试时以游客、普通登录用户、创作者和管理员四类角色为视角，围绕“注册登录、内容浏览、内容发布、互动通知、智能问答、票务查询、后台知识管理”七条主流程设计测试用例。每个用例包含测试编号、测试目标、前置条件、执行步骤和预期结果，以保证测试具有可复现性。

在用户认证测试中，重点验证注册成功后是否自动创建资料记录、登录后是否正确生成会话、错误密码是否能触发提示、被封禁用户是否会被拒绝访问。社区业务测试则重点覆盖帖子发布、图片上传、评论回复、点赞收藏、通知生成、关注与取关等典型交互流程。AI 模块测试重点验证知识问答可用性、历史会话保存、图片分析结果返回、模型切换有效性以及未发布知识库不可被普通用户调用。票务模块测试重点验证站点输入校验、余票查询成功、价格按需拉取与异常情况下的错误提示。

从执行方式看，现阶段系统功能测试以人工执行为主，结合浏览器控制台日志、Supabase 数据库变化、Storage 文件写入与后端日志进行结果核对。这种方式虽然自动化程度有限，但能够较好覆盖毕业设计阶段的复杂交互流程，尤其适用于涉及第三方接口、实时订阅和 AI 输出内容校验的场景。

#### 5.1.2 非功能性测试方案

非功能性测试主要包括性能测试、安全性测试、兼容性测试与可用性测试。性能测试关注页面首屏渲染速度、AI 问答响应时间、数据库查询延迟和票务接口返回耗时；安全性测试重点检查认证访问控制、RLS 权限策略、后台管理员权限隔离和对象存储上传约束；兼容性测试关注在主流 Chromium 浏览器环境下页面布局、富文本编辑、图片上传与语音输入功能是否正常；可用性测试则关注用户是否能够在较少学习成本下完成发帖、评论、提问与票务查询等高频操作。

针对不同能力，系统采用了不同测试工具和方法：前端页面性能可通过浏览器 DevTools 与 Lighthouse 进行评估；后端接口响应可通过浏览器网络面板、Postman 或日志埋点方式观测；Supabase Realtime 与数据库触发器行为可通过控制台日志和数据表变化进行验证；AI 助手结果则通过管理员问答测试机制和人工比对方式检验答案相关性与可读性。通过上述组合式方案，可以较全面地评估系统在真实使用环境中的综合表现。

### 5.2 系统功能测试结果与分析

#### 5.2.1 核心业务流程测试分析

在用户认证流程测试中，系统能够正确完成注册、登录、退出与路由访问控制。新用户注册后，数据库中的 `auth.users` 与 `profiles` 表会同步生成对应记录；登录成功后，受保护页面可正常访问；当未登录用户访问发布页、通知页、个人中心或后台页时，系统会自动跳转到登录页。针对被封禁用户的测试表明，系统不仅在路由进入前进行角色校验，还通过实时订阅监听 `profiles.role` 的变化，一旦管理员将当前用户标记为 `banned`，前端即可即时弹窗并强制退出登录，说明认证与权限控制逻辑具备较高的一致性。

在社区流程测试中，帖子发布、评论回复、点赞收藏和关注操作均能正常完成。用户发布图文内容后，媒体资源先上传至 `media` 存储桶，再写入帖子记录；社区首页能够按发布时间加载已发布内容，并根据分类与兴趣标签进行推荐排序；评论模块能够生成楼中楼结构，且评论被回复时可自动生成通知；点赞和收藏按钮在同一用户重复点击时能正确在“插入”和“删除”状态间切换，验证了 `interactions` 唯一约束与前端切换逻辑的有效性。通知页可以根据“评论和@”“赞和收藏”“新增关注”三个标签页正确聚合对应消息，说明数据库触发器与前端通知服务联动正常。

在票务流程测试中，用户输入出发地、目的地与日期后，系统能够将城市名称转换为站点代码，并通过边缘函数返回车次、时间、历时和各类座席余票信息；当用户点击某一座席时，系统能够再次调用价格接口按需返回票价数据。对于 12306 接口异常、站点名称错误或参数缺失等情况，页面也能给出明确提示信息，没有出现空白页或阻塞式报错，说明票务模块在接口异常处理方面具有较好的健壮性。

#### 5.2.2 AI助手与社交功能测试分析

AI 助手测试结果表明，系统在多轮问答、会话历史保存、知识库门禁控制和多模态扩展方面均能达到预期效果。用户首次提问时，系统可自动创建新会话；后续问答消息会持续写入消息记录，并在历史侧边栏中按最近更新时间展示；当用户上传图片时，系统能够先完成对象存储写入，再调用图像分析接口返回描述结果；当用户切换不同聊天模型时，请求参数能够传递到后端路由层，实现模型动态解析。对于未发布或未通过测试的知识库，系统能够向用户返回“当前知识库尚未启用”的提示，而不会继续执行 RAG 问答，这表明管理员测试审批机制真正参与了前台访问控制。

从社交互动角度看，社区模块测试结果证明了“行为写入 - 触发器执行 - 通知回流 - 页面展示”的链路是通畅的。评论他人帖子后，帖子作者可在通知页面收到评论通知；点赞帖子或点赞评论后，对应内容作者也可收到点赞通知；新增关注后，被关注用户可以立即在“新增关注”页签中看到记录，并可直接执行回关操作。结合 Presence 在线状态与封禁订阅能力可知，系统已经初步形成了基于 Supabase Realtime 的互动增强机制，为后续扩展聊天室、实时私信或在线协同场景提供了基础。

总体而言，AI 助手模块提高了平台的智能服务能力，社交模块增强了用户之间的内容互动与关系连接，两者协同提升了系统粘性与使用深度。但测试中也发现，AI 模块在极端情况下仍会受到外部模型响应时间波动影响，票务模块也受第三方平台稳定性约束，因此后续仍需在缓存、限流和降级策略方面进一步优化。

### 5.3 系统性能评估与优化

#### 5.3.1 响应时间与并发性能测试

在性能评估方面，本文从页面加载性能与接口响应性能两个维度展开测试。页面层面可通过 Lighthouse 与浏览器性能面板观察首页、社区页、AI 助手页和票务页的首屏加载情况。结合项目现有测试记录，首页 FCP 约为 1.2s、LCP 约为 1.8s，社区页 FCP 约为 1.5s、LCP 约为 2.1s，AI 助手页 FCP 约为 1.3s、LCP 约为 1.9s，票务页 FCP 约为 1.1s、LCP 约为 1.6s，说明系统在静态资源控制和页面结构设计方面总体较为轻量，能够满足一般单页应用的交互需求。

接口性能方面，数据库查询接口通常能够在 100ms 至 300ms 范围内完成响应，其中帖子列表查询、通知查询和资料读取等读操作响应较快；涉及第三方接口代理的票务查询和价格查询在网络状态稳定时通常可控制在亚秒级到数秒级之间；AI 助手由于需要经历模型推理与检索增强，响应时间明显高于普通业务接口，但在知识库命中且上下文规模适中的情况下，依然可以保持在用户可接受范围内。以项目既有测试记录为例，常规业务接口 P95 可控制在 200ms 左右，票务查询接口 P95 约为 300ms 量级，AI 助手问答接口 P95 通常在 1.5s 至 3s 区间，符合“普通操作快速返回、复杂智能任务允许稍长等待”的交互特征。

并发性能测试方面，系统的主要压力点集中在社区内容读取、通知聚合以及 AI 助手请求。对于社区模块，由于帖子、评论和互动查询本质上属于以读为主的结构化访问，Supabase PostgreSQL 与索引能够较好支撑中等规模并发访问；对于 AI 问答，由于请求路径涉及模型调用，系统更容易受外部模型服务吞吐上限影响，因此更适合采用“控制并发 + 异步处理 + 提示加载状态”的策略。结合本项目定位，系统已能够满足课程设计和中小规模示范系统的并发需求。

#### 5.3.2 基于云原生特性的优化实践

为提升系统整体性能与稳定性，项目在实现过程中结合 Supabase 的云原生能力进行了多项优化。首先，在数据库层面对向量检索字段建立 HNSW 索引、对中文文本建立 PGroonga 索引，并通过 RRF 融合减少单一检索方式带来的召回偏差，从而提升 RAG 场景下的检索效率和答案相关性。其次，在对象存储层采用“资源上传后只回写 URL”的方式，避免将大体积媒体文件直接写入关系数据库；社区图片、头像和 AI 上传图片均通过 Storage 与 CDN 能力完成分发，有助于减轻主数据库压力。

在接口层面，项目充分利用了边缘函数的近源执行能力。票务查询、价格查询、图像分析、图像生成与地图代理等服务均通过 Edge Functions 进行封装，这一做法不仅解决了跨域和密钥暴露问题，还使第三方服务访问可以在云端统一治理。对于 12306 这类不稳定接口，边缘函数内部实现了请求头伪装、非 JSON 响应识别和降级返回逻辑，保证了前端不会因单次外部失败而完全失去可用性。

在前端层面，项目通过 Vite 的按需构建、路由级动态加载、组件拆分与本地状态封装降低了初始加载开销；图片与媒体资源通过对象存储直链分发，减少了后端转发压力；输入提交过程中通过防抖、重复提交拦截和加载态提示优化了用户感知性能。对 AI 助手而言，系统还通过会话持久化和模型列表动态获取，避免了每次进入页面都重新初始化全部上下文数据。

在安全与稳定性协同优化方面，系统启用了行级安全策略、角色鉴权与管理员发布审批机制。虽然这些机制本质上是安全设计，但也间接提升了系统运行稳定性，避免了非法写入、越权访问和未经测试知识污染线上问答结果的问题。可以认为，本系统的优化并非只停留在“加快速度”，而是通过云原生数据服务、边缘计算与权限控制的协同，实现了性能、可维护性与安全性的综合平衡。

综上所述，系统在当前规模下已经具备较好的页面加载性能、业务接口响应效率与云服务扩展能力。未来若面向更大规模用户场景部署，可进一步引入查询缓存、异步任务队列、向量检索分层索引、对象存储图片压缩与更完善的监控告警机制，以持续提升平台的整体服务质量。
